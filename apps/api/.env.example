DATABASE_URL="postgresql://nomadcore:nomadcore@localhost:5432/nomadcore"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="30d"
PORT=3001
# OTP: на старте заглушка (код логируется), SMS-провайдер КР подключаем позже
OTP_MODE="stub"

# --- Фаза 0.2: Telegram-бот ---
# stub — сообщения логируются вместо отправки; real — через api.telegram.org
TELEGRAM_MODE="stub"
TELEGRAM_BOT_TOKEN=""
# Секрет для проверки webhook (X-Telegram-Bot-Api-Secret-Token)
TELEGRAM_WEBHOOK_SECRET="change-me"
