/**
 * Опции самого vue-i18n. Модульные настройки (locales, langDir, strategy)
 * остаются в nuxt.config — здесь только то, что модуль передаёт внутрь.
 *
 * fallbackLocale нужен двум местам:
 *  · кабинет госагентства ведётся только по-русски — это официальная
 *    отчётность, и переводить её на кыргызский в интерфейсе бессмысленно,
 *    пока агентство само публикует статистику по-русски;
 *  · en.json остался с прежним набором ключей и новыми не пополнялся.
 *
 * Без него отсутствующий ключ печатается на экране путём вида
 * «gov.col.occupancy» — на государственном экране это недопустимо.
 */
export default defineI18nConfig(() => ({
	legacy: false,
	fallbackLocale: "ru",
}))
